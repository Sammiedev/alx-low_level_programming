#include <stdio.h>
/**
 * Main - A program that print a line using the function printf funtion
 * Return: 0 (Success)
 */

int main(void)
{
	printf("program that prints exactly with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
